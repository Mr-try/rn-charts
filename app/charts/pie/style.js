import {createStyle} from 'app/utils'

const styles = createStyle({
  container: {
    marginTop: 100,
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
})
export default styles