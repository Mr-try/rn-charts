import {createStyle} from 'app/utils'

const styles = createStyle({
  container: {
    marginTop: 12,
    backgroundColor: '#fff'
  },
  legentWrap: {
    position: 'absolute',
    top: 0,
    left: 0
  }
})
export default styles